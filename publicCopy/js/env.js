//Here we are going to store all things that are arbitrary and prone to change

//Editor Constants
export const DEFAULT_THUMB_OPTIONS = Object.freeze({
    width: 640,
    height: 480,
    type: "image/webp",
    quality: 0.8
});